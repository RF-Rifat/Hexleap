export interface SportEvent {
  imgLink: string;
  title: string;
  event: string;
  sport: string;
  totalEvents: number;
}
export interface SwiperDataItem {
  title: string;
  date: string;
  time: string;
  location: string;
  imageUrl: string;
  btnTag: string;
}